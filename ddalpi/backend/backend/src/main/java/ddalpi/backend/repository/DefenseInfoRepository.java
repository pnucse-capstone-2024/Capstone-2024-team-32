package ddalpi.backend.repository;

import ddalpi.backend.domain.DefenseInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DefenseInfoRepository extends JpaRepository<DefenseInfo, Long> {
}
